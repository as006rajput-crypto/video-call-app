# Video Call App

A real-time video calling application built with Node.js, Express, and Socket.io.

## How to Run

1. Install dependencies: `npm install`
2. Start server: `node server/server.js`
3. Open browser: `http://localhost:3000`
